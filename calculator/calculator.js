const add = (x, y) => {

    return +x + +y;

}

const sub = (x, y) => {

    return +x - +y;

}

const multi = (x, y) => {

    return +x * +y;

}

const divide = (x, y) => {

    return +x / +y;

}


module.exports = { add, sub, multi, divide };

